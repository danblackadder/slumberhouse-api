export enum OrganizationRole {
  OWNER = 'owner',
  ADMIN = 'admin',
  BASIC = 'basic',
}

export enum GroupRole {
  ADMIN = 'admin',
  BASIC = 'basic',
  EXTERNAL = 'external',
}

export enum UserStatus {
  INVITED = 'invited',
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}
